# -----------------------------------------------------------------------------
# PLACEHOLDER FILE — replace the value below with your OWN, NEW Groq API key.
# -----------------------------------------------------------------------------
# Do NOT commit this file with a real key to a public GitHub repo.
# On Streamlit Community Cloud, instead paste this content (with your real key)
# into: App settings -> Secrets, in the web dashboard. That keeps the real key
# out of your git history entirely.
#
# Get a key at: https://console.groq.com/keys
# -----------------------------------------------------------------------------

GROQ_API_KEY = "PASTE_YOUR_NEW_GROQ_API_KEY_HERE"
